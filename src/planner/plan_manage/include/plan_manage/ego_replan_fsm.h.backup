/**
 * @file ego_replan_fsm.h
 * @brief EGO Replanning Finite State Machine with Piecewise Bezier Curves
 *        Enhanced with mandatory waypoint reaching functionality
 * 
 * 核心改进: 航点强制到达机制
 * - 航点成为无人机真实轨迹必须到达的点
 * - 基于无人机实际位置检测航点到达
 * - 只有实际到达航点后才切换到下一个航点
 */

#ifndef _REBO_REPLAN_FSM_H_
#define _REBO_REPLAN_FSM_H_

#include <Eigen/Eigen>
#include <algorithm>
#include <iostream>
#include <nav_msgs/Path.h>
#include <sensor_msgs/Imu.h>
#include <ros/ros.h>
#include <std_msgs/Empty.h>
#include <std_msgs/Int32.h>
#include <std_msgs/Float32.h>
#include <vector>
#include <visualization_msgs/Marker.h>

#include <bezier_opt/bezier_optimizer.h>
#include <plan_env/grid_map.h>
#include <ego_planner/Bezier.h>
#include <ego_planner/DataDisp.h>
#include <plan_manage/planner_manager.h>
#include <traj_utils/planning_visualization.h>

using std::vector;

namespace ego_planner
{

  class EGOReplanFSM
  {

  private:
    /* FSM states */
    enum FSM_EXEC_STATE
    {
      INIT,
      WAIT_TARGET,
      GEN_NEW_TRAJ,
      REPLAN_TRAJ,
      EXEC_TRAJ,
      EMERGENCY_STOP,
      REACHING_WAYPOINT  // 新增状态：正在逼近航点
    };
    enum TARGET_TYPE
    {
      MANUAL_TARGET = 1,
      PRESET_TARGET = 2,
      REFENCE_PATH = 3
    };

    /* Planning utils */
    EGOPlannerManager::Ptr planner_manager_;
    PlanningVisualization::Ptr visualization_;
    ego_planner::DataDisp data_disp_;

    /* Parameters */
    int target_type_;
    double no_replan_thresh_, replan_thresh_;
    double goal_reach_thresh_;           // 最终目标点到达判断阈值
    double waypoint_reach_thresh_;       // 强制航点到达阈值（核心参数）
    double waypoints_[50][3];
    int waypoint_num_;
    double planning_horizen_, planning_horizen_time_;
    double emergency_time_;

    /* ========== 航点强制到达系统 ========== */
    int current_waypoint_idx_;           // 当前正在前往的航点索引 (0-based)
    bool waypoint_reached_;              // 当前航点是否已被实际到达
    std::vector<Eigen::Vector3d> all_waypoints_;  // 存储所有航点
    bool use_mandatory_waypoints_;       // 是否启用强制航点到达模式
    Eigen::Vector3d current_target_waypoint_;  // 当前目标航点
    bool all_waypoints_completed_;       // 所有航点是否已完成
    int waypoint_replan_count_;          // 当前航点的重规划次数
    int max_waypoint_replan_attempts_;   // 每个航点的最大重规划次数
    double waypoint_approach_time_;      // 接近航点的时间计数
    double max_waypoint_approach_time_;  // 单个航点最大允许接近时间
    ros::Time last_waypoint_check_time_; // 上次航点检查时间

    /* Planning data */
    bool trigger_, have_target_, have_odom_, have_new_target_;
    FSM_EXEC_STATE exec_state_;
    int continously_called_times_{0};

    Eigen::Vector3d odom_pos_, odom_vel_, odom_acc_;
    Eigen::Quaterniond odom_orient_;

    Eigen::Vector3d init_pt_, start_pt_, start_vel_, start_acc_, start_yaw_;
    Eigen::Vector3d end_pt_, end_vel_;
    Eigen::Vector3d local_target_pt_, local_target_vel_;
    int current_wp_;

    bool flag_escape_emergency_;

    /* ROS utils */
    ros::NodeHandle node_;
    ros::Timer exec_timer_, safety_timer_, waypoint_check_timer_;  // 航点检测定时器
    ros::Subscriber waypoint_sub_, odom_sub_;
    ros::Publisher replan_pub_, new_pub_, bezier_pub_, data_disp_pub_;
    ros::Publisher traj_path_pub_;  // 发布Path格式的轨迹（用于多机协同）
    ros::Publisher waypoint_status_pub_;  // 发布航点状态
    ros::Publisher current_waypoint_pub_; // 发布当前航点索引
    ros::Publisher waypoint_distance_pub_; // 发布到当前航点的距离

    /* Helper functions */
    bool callReboundReplan(bool flag_use_poly_init, bool flag_randomPolyTraj);
    bool callEmergencyStop(Eigen::Vector3d stop_pos);
    bool planFromCurrentTraj();
    void publishTrajectoryPath();  // 发布轨迹Path消息

    void changeFSMExecState(FSM_EXEC_STATE new_state, string pos_call);
    std::pair<int, EGOReplanFSM::FSM_EXEC_STATE> timesOfConsecutiveStateCalls();
    void printFSMExecState();

    void planGlobalTrajbyGivenWps();
    void planNextWaypoint();             // 规划到下一个航点
    void getLocalTarget();
    
    /* ========== 航点强制到达核心函数 ========== */
    /**
     * @brief 检查无人机实际位置是否到达当前目标航点
     * @return true 如果实际位置在航点阈值范围内
     */
    bool checkWaypointReached();
    
    /**
     * @brief 前进到下一个航点，更新目标
     * @return true 如果还有下一个航点
     */
    bool advanceToNextWaypoint();
    
    /**
     * @brief 规划到当前目标航点的全局轨迹
     * @return true 如果规划成功
     */
    bool planToCurrentWaypoint();
    
    /**
     * @brief 初始化航点追踪系统
     */
    void initWaypointTracking();
    
    /**
     * @brief 获取当前目标航点
     * @return 当前目标航点的3D位置
     */
    Eigen::Vector3d getCurrentTargetWaypoint();
    
    /**
     * @brief 检查是否所有航点都已完成
     * @return true 如果所有航点都已到达
     */
    bool isAllWaypointsCompleted();
    
    /**
     * @brief 发布航点状态信息
     */
    void publishWaypointStatus();
    
    /**
     * @brief 规划从当前位置直接到航点的轨迹（忽略后续航点）
     * @return true 如果规划成功
     */
    bool planDirectToWaypoint();

    /**
     * @brief 可视化当前目标航点
     */
    void visualizeCurrentWaypoint();

    /* ROS callbacks */
    void execFSMCallback(const ros::TimerEvent &e);
    void checkCollisionCallback(const ros::TimerEvent &e);
    void waypointCheckCallback(const ros::TimerEvent &e);  // 航点检测回调
    void waypointCallback(const nav_msgs::PathConstPtr &msg);
    void odometryCallback(const nav_msgs::OdometryConstPtr &msg);

    bool checkCollision();

  public:
    EGOReplanFSM() {}
    ~EGOReplanFSM() {}

    void init(ros::NodeHandle &nh);

    EIGEN_MAKE_ALIGNED_OPERATOR_NEW
  };

} // namespace ego_planner

#endif
